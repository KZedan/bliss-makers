<!Doctype html>
<html>
<head>
  <link rel="stylesheet" type="text/css" href="skeleton.css">

</head>
<body>
<div class="header-site">

    <div class="navbar">
      <div class="dropdown">
        <button class="dropbtn">Menu
          <i class="fa fa-caret-down"></i>
        </button>
        <div class="dropdown-content">
          <a href="/spaces">Listings</a>
          <a href="/requests">Requests</a>
          <a href="/logout">Logout</a>
        </div>
      </div>
    </div>

<center><h1> Welcome to Bliss, <%= @user.user_name %>! </h1>

</div>


    <center><h2> Listings </h2></center>
      <% @spaces.each do |space| %>
      <div class="container">

               <div align="center" class="one-third column value" nowrap>
              <font size="3"><%= space.space_name %>
              </div>
              <div align="left" class="one-third column value" nowrap style="line-height:1.0; font-size: 8px;">
              <i><font size="1"><%= space.description %></i>
              </div>
              <div align="right" class="end-small columnn value" nowrap >
              <br>£<%= space.price %> per night.
              </div>
          </div>

      <% end %>
</body>
